version: '3'

services:
  db:
    image: postgres
  web:
    build: .
    environment:
      ALLOWED_HOSTS: localhost 
      SECRET_KEY: abcd
      DEBUG: "True"
      SENTRY_DSN: ""
      B_DATABASE_URL: postgres://postgres:@db:5432/postgres
    command: ./run_dev_server.sh
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db
